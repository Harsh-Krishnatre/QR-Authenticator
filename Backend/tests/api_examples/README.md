This folder contains example requests and expected responses for each API endpoint.

Structure:
- `auth/<endpoint>/request_<case>.json` - example request payload (or example URL for GET)
- `auth/<endpoint>/response_<case>.json` - example response JSON from the server for that case

These examples follow the backend's validation and response shapes.
